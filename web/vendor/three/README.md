three.js r186 (0.186.1), MIT — see LICENSE. Copied unmodified from
https://cdn.jsdelivr.net/npm/three@0.186.1/ (`build/three.module.min.js`,
`build/three.core.min.js` saved as `three.core.js` because the module imports that name,
and the four `examples/jsm` addons neko uses) so the page works offline.
